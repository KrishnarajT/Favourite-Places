import React from "react";
import { View, StyleSheet, Text } from "react-native";
import CText from "../ui/CText";

const PlaceForm = () => {
	return (
		<View>
			<CText>The Place Form</CText>
		</View>
	);
};

const styles = StyleSheet.create({});

export default PlaceForm;
